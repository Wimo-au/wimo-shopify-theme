<div class="announcement">{{ section.settings.text }}</div>

{% schema %}
{
  "name": "Announcement bar",
  "settings": [
    {
      "type": "text",
      "id": "text",
      "label": "Text",
      "default": "Complimentary shipping on orders over $120 · Poured across Australia"
    }
  ]
}
{% endschema %}
